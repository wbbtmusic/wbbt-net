version: '3.8'

services:
  app:
    build: .
    restart: always
    container_name: wbbt-app
    ports:
      - "3005:80"
    # Port 3005 is exposed to the host.
    # Point your existing Caddy to: reverse_proxy localhost:3005
